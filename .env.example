# Test Credentials - Copy to .env and fill in actual values
# NEVER commit .env to git - it's in .gitignore

# Valid test account credentials (required)
TEST_EMAIL_VALID=your-test-email@example.com
TEST_PASSWORD_VALID=your-test-password

# Note: Invalid credentials are hardcoded in utils/env.ts - no need to configure them
